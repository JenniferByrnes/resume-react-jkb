import React from 'react'
import PhotoList from '../PhotoList'

function Portfolio() {

  return (
    <section>
      <h1>My Projects</h1>
      {//<h1>{(name)}</h1>
      }
      {//<p>{description}</p>
      }
      <PhotoList />
    </section>
  )
}

export default Portfolio;